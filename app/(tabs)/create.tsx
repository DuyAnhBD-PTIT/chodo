// This is a placeholder screen for the create tab
// The actual create functionality is handled by the CreatePostSheet bottom sheet
// which opens when this tab is pressed

import { View } from "react-native";

export default function CreateScreen() {
  return <View />;
}
